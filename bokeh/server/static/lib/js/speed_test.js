// Generated by CoffeeScript 1.3.1
(function() {

  test('speed_test', function() {
    var container, data, data_source, plot1, temp, x, y;
    expect(0);
    x = _.range(1500);
    y = _.range(1500);
    data = (function() {
      var _i, _len, _ref, _results;
      _ref = _.zip(x, y);
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        temp = _ref[_i];
        _results.push({
          'x': temp[0],
          'y': temp[1]
        });
      }
      return _results;
    })();
    data_source = Bokeh.Collections['ObjectArrayDataSource'].create({
      data: data
    });
    container = Bokeh.Collections['InteractiveContext'].create({}, {
      'local': true
    });
    plot1 = Bokeh.line_plot(container, data_source, 'x', 'y');
    container.set({
      'children': [plot1.ref()]
    });
    window.myrender = function() {
      var view;
      view = new container.default_view({
        'model': container
      });
      view.render();
      return window.view = view;
    };
    return _.defer(window.myrender);
  });

}).call(this);
