#!/bin/bash

# XXX: mypy on 3.5 is way too old to work correctly
conda install -n testenv -c conda-forge --yes --quiet 'mypy>=0.720'

# XXX: until channels is avialable on conda-forge
pip install channels

set -e # exit on error
set -x # echo commands

# install NPM dependencies
pushd bokehjs
npm install --no-save --no-progress
popd
