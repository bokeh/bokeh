{expect} = require "chai"

{Spacer} = require("models/layouts/spacer")

describe "SpacerView", ->

  beforeEach ->
    @spacer = new Spacer()

describe "Spacer", ->

  beforeEach ->
    @spacer = new Spacer()
